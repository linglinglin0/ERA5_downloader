# ERA5下载器 Git 提交方案

## 📋 当前状态分析

### 当前分支
- **主分支**: `master`
- **远程仓库**: `origin/master`

### 未提交的文件

#### 核心功能文件
- `ERA5download_GUI_v2_fixed.py` - v2修复版（连接池修复）
- `ERA5download_GUI_v2_auto_restart.py` - v2.1自动重启版
- `ERA5download_GUI_v3.py` - v3高级版
- `ERA5_AutoRecovery.py` - v3.1独立监控版
- `era5_performance_monitor.py` - 性能监控器
- `era5_advanced_features.py` - 高级特性代码

#### 配置和工具
- `pyproject.toml`, `requirements.txt`, `uv.lock` - 依赖配置
- 各种启动脚本（.bat文件）

#### 文档
- 各种功能说明、bug修复说明、使用指南等（20+个md文件）

---

## 🎯 推荐的分支策略

### 方案A：两个独立的功能分支（推荐）

```
master (主分支)
  ├── feature/auto-restart-v2 (v2自动重启功能)
  └── feature/advanced-features-v2 (v2高级特性集成)
```

**优点**：
- ✅ 功能隔离清晰
- ✅ 便于code review
- ✅ 可以独立合并到主分支

---

## 📝 详细提交计划

### 第一部分：在主分支上先提交基础改进

#### Commit 1: 更新项目依赖和构建工具

```
分支: master
文件: pyproject.toml, requirements.txt, uv.lock
描述: build: 更新项目依赖到uv包管理器和CustomTkinter
```

**命令**：
```bash
git add pyproject.toml requirements.txt uv.lock
git commit -m "build: 更新项目依赖到uv包管理器和CustomTkinter

- 迁移到 uv 包管理器
- 更新 CustomTkinter 到最新版本
- 添加项目配置文件
```

---

### 第二部分：创建并提交 v2 自动重启功能分支

#### 步骤1：创建分支

```bash
git checkout -b feature/auto-restart-v2
```

#### Commit 2: 添加 v2.1 自动重启版

```
分支: feature/auto-restart-v2
文件: ERA5download_GUI_v2_auto_restart.py
描述: feat: 添加v2.1智能自动重启版下载器
```

**命令**：
```bash
git add ERA5download_GUI_v2_auto_restart.py
git commit -m "feat(v2.1): 添加智能自动重启版下载器

核心功能：
- 基于v2_fixed版本，保留所有性能修复
- 内置速度监控，实时检测下载速度
- 智能低速检测：速度 < 500 KB/s 持续120秒自动重启
- 断点续传无缝恢复
- 完全自动化：无需手动确认，支持重启后自动开始下载
- 防卡死优化：在守护线程中执行重启，避免GUI阻塞

技术实现：
- 使用 threading.Thread 守护线程执行重启
- 优雅退出机制：self.quit() + sys.exit()
- 标志文件机制：.era5_auto_start_flag
- 重启统计和日志记录

相关文件：
- ERA5download_GUI_v2_auto_restart.py (主程序)
- 启动v2自动重启版.bat (启动脚本)
"
```

#### Commit 3: 添加 v2.1 启动脚本

```
分支: feature/auto-restart-v2
文件: 启动v2自动重启版.bat
描述: feat: 添加v2.1启动脚本
```

**命令**：
```bash
git add "启动v2自动重启版.bat"
git commit -m "feat: 添加v2.1智能自动重启版启动脚本

- 提供友好的命令行界面
- 显示版本特性和使用说明
- 自动启动 ERA5download_GUI_v2_auto_restart.py
"
```

#### Commit 4: 添加 v2.1 文档

```
分支: feature/auto-restart-v2
文件: v2相关的文档
描述: docs: 添加v2.1完整使用文档
```

**命令**：
```bash
git add \
  "v2.1智能自动重启更新说明.md" \
  "v2.1完全自动重启功能完成.md" \
  "v2.1时间计算Bug修复说明.md" \
  "v2.1自动重启卡死Bug修复说明.md" \
  "v2.1自动重启卡死最终修复.md"

git commit -m "docs(v2.1): 添加完整的v2.1自动重启功能文档

文档内容：
- v2.1智能自动重启更新说明 - 功能介绍和使用指南
- v2.1完全自动重启功能完成 - 功能完成总结
- v2.1时间计算Bug修复说明 - 时间计算bug分析和修复
- v2.1自动重启卡死Bug修复说明 - 卡死问题分析和修复
- v2.1自动重启卡死最终修复 - 彻底解决卡死问题
"
```

#### Commit 5: 提推送到远程

```bash
git push -u origin feature/auto-restart-v2
```

---

### 第三部分：创建并提交 v2 高级特性集成分支

#### 步骤1：切回主分支并创建新分支

```bash
git checkout master
git checkout -b feature/advanced-features-v2
```

#### Commit 6: 添加性能监控器

```
分支: feature/advanced-features-v2
文件: era5_performance_monitor.py
描述: feat: 添加性能监控器GUI应用
```

**命令**：
```bash
git add era5_performance_monitor.py
git commit -m "feat(monitor): 添加性能监控器GUI应用

核心功能：
- 实时监控下载速度和累计下载量
- SQLite数据库持久化存储
- 三种图表：速度曲线、累计下载量、系统资源
- 支持双Y轴显示（GB/MB）
- 智能单位格式化（KB/s, MB/s, GB/s）
- 导出CSV数据功能
- 网络接口I/O监控（psutil）
- 文件大小监控备用方案

技术特性：
- CustomTkinter现代化GUI
- Matplotlib实时图表
- 每2秒自动刷新
- 滑动窗口平均（10个样本）
- 自动回退机制

相关文件：
- era5_performance_monitor.py (主程序)
- 启动性能监控.bat (启动脚本)
"
```

#### Commit 7: 添加性能监控器启动脚本

```bash
git add "启动性能监控.bat"
git commit -m "feat: 添加性能监控器启动脚本"
```

#### Commit 8: 添加 v3.0 高级特性版

```
分支: feature/advanced-features-v2
文件: ERA5download_GUI_v3.py
描述: feat: 添加v3.0高级特性集成版
```

**命令**：
```bash
git add ERA5download_GUI_v3.py
git commit -m "feat(v3.0): 添加高级特性集成版下载器

核心功能：
- 基于v2版本的所有功能
- 自适应限流算法
- 连接池动态管理
- 性能监控数据库集成
- 实时统计和日志
- 连接池重建机制

性能优化：
- 限流算法：避免触发S3限流
- 连接池优化：最大池连接数 = 线程数 × 4
- 定期重建连接池：防止连接健康度下降
- 智能延迟：指数退避 + 抖动

相关文件：
- ERA5download_GUI_v3.py (主程序)
- 启动程序_v3.bat (启动脚本)
"
```

#### Commit 9: 添加 v3.0 启动脚本

```bash
git add "启动程序_v3.bat"
git commit -m "feat: 添加v3.0启动脚本"
```

#### Commit 10: 添加 v3.0 文档

```bash
git add \
  "v3.0新功能说明.md" \
  "v3.0性能优化说明.md"

git commit -m "docs(v3.0): 添加v3.0完整功能文档

文档内容：
- v3.0新功能说明 - 功能介绍和特性详解
- v3.0性能优化说明 - 性能优化方案和效果
"
```

#### Commit 11: 添加 v3.1 独立监控版

```bash
git add ERA5_AutoRecovery.py
git commit -m "feat(v3.1): 添加独立智能自动恢复系统

核心功能：
- 独立的监控和重启脚本
- 读取性能监控器数据库
- 自动检测低速并重启v3.0下载器
- 完全分离的架构：监控 + 下载器
- 可配置阈值和持续时间

技术实现：
- SpeedMonitor 类：速度监控逻辑
- AutoRecoveryDownloader 类：重启管理
- 使用 win32api 发送关闭消息
- 进程管理：停止 → 等待 → 启动

相关文件：
- ERA5_AutoRecovery.py (主程序)
- 启动智能恢复版.bat (启动脚本)
- 智能自动恢复功能说明.md (文档)
"
```

#### Commit 12: 添加 v3.1 启动脚本和文档

```bash
git add "启动智能恢复版.bat"
git add "智能自动恢复功能说明.md"
git commit -m "feat: 添加v3.1独立监控版启动脚本和文档"
```

#### Commit 13: 添加高级特性代码

```bash
git add era5_advanced_features.py
git commit -m "feat: 添加高级特性模块

包含各种高级功能和工具代码
"
```

#### Commit 14: 添加监控相关文档

```bash
git add \
  "网络接口监控说明.md" \
  "智能恢复功能Bug修复说明.md" \
  "版本对比指南_v2.1_vs_v3.1.md"

git commit -m "docs: 添加监控和版本对比文档

文档内容：
- 网络接口监控说明 - psutil监控实现
- 智能恢复功能Bug修复说明 - bug修复记录
- 版本对比指南_v2.1_vs_v3.1 - 两个版本详细对比
"
```

#### Commit 15: 提推送到远程

```bash
git push -u origin feature/advanced-features-v2
```

---

### 第四部分：提交通用文档和工具

#### 步骤1：切回主分支

```bash
git checkout master
```

#### Commit 16: 添加通用文档

```bash
git add \
  "README_总览.md" \
  "版本总览README.md" \
  "UV包管理器使用指南.md"

git commit -m "docs: 添加项目总览文档

文档内容：
- README_总览.md - 项目概览
- 版本总览README.md - 所有版本介绍
- UV包管理器使用指南.md - uv使用说明
"
```

#### Commit 17: 添加工具脚本

```bash
git add \
  setup_env_uv.bat \
  setup_env.bat \
  create_release.py \
  create_release_clean.py \
  check_releases.py \
  check_process.py \
  diagnostic_tool.py

git commit -m "tools: 添加项目工具脚本

工具列表：
- 环境设置脚本（setup_env.bat, setup_env_uv.bat）
- Release创建脚本（create_release.py, create_release_clean.py）
- 检查脚本（check_releases.py, check_process.py）
- 诊断工具（diagnostic_tool.py）
"
```

#### Commit 18: 添加其他启动脚本

```bash
git add \
  "启动v2自动重启版.bat" \
  "启动性能监控.bat" \
  "启动程序_v3.bat" \
  "启动智能恢复版.bat"

git commit -m "feat: 添加所有版本的启动脚本

包括：
- v2.1 自动重启版
- 性能监控器
- v3.0 高级版
- v3.1 独立监控版
"
```

#### Commit 19: 添加其他文档

```bash
git add \
  "监控器修复说明.md" \
  "断点续传监控修复说明.md" \
  "网络接口监控说明.md" \
  "智能恢复功能Bug修复说明.md"

git commit -m "docs: 添加监控和修复说明文档"
```

#### Commit 20: 提交数据库和日志配置

```bash
git add .gitignore

git commit -m "chore: 更新.gitignore文件

- 添加 era5_performance.db
- 添加 *.log 文件
- 添加临时文件和配置文件
"
```

#### Commit 21: 推送主分支

```bash
git push origin master
```

---

## 🚀 执行计划

### 准备工作

```bash
# 1. 检查当前状态
cd "D:\wzl\ERA5下载软件"
git status
git branch -a

# 2. 确保远程仓库已配置
git remote -v

# 3. 如果需要配置GitHub token
# 设置环境变量或使用 SSH
```

---

### 执行提交（按顺序）

#### 第一阶段：主分支基础改进

```bash
# 1. 更新依赖
git add pyproject.toml requirements.txt uv.lock
git commit -m "build: 更新项目依赖到uv包管理器和CustomTkinter

- 迁移到 uv 包管理器
- 更新 CustomTkinter 到最新版本
- 添加项目配置文件
"
git push origin master
```

---

#### 第二阶段：v2自动重启功能分支

```bash
# 2. 创建分支
git checkout -b feature/auto-restart-v2

# 3. 添加v2.1主程序
git add ERA5download_GUI_v2_auto_restart.py
git commit -m "feat(v2.1): 添加智能自动重启版下载器

核心功能：
- 基于v2_fixed版本，保留所有性能修复
- 内置速度监控，实时检测下载速度
- 智能低速检测：速度 < 500 KB/s 持续120秒自动重启
- 断点续传无缝恢复
- 完全自动化：无需手动确认，支持重启后自动开始下载
- 防卡死优化：在守护线程中执行重启，避免GUI阻塞

技术实现：
- 使用 threading.Thread 守护线程执行重启
- 优雅退出机制：self.quit() + sys.exit()
- 标志文件机制：.era5_auto_start_flag
- 重启统计和日志记录
"

# 4. 添加启动脚本
git add "启动v2自动重启版.bat"
git commit -m "feat: 添加v2.1启动脚本"

# 5. 添加文档
git add \
  "v2.1智能自动重启更新说明.md" \
  "v2.1完全自动重启功能完成.md" \
  "v2.1时间计算Bug修复说明.md" \
  "v2.1自动重启卡死Bug修复说明.md" \
  "v2.1自动重启卡死最终修复.md"

git commit -m "docs(v2.1): 添加完整的v2.1自动重启功能文档"

# 6. 推送分支
git push -u origin feature/auto-restart-v2
```

---

#### 第三阶段：v2高级特性集成分支

```bash
# 7. 切回主分支
git checkout master

# 8. 创建分支
git checkout -b feature/advanced-features-v2

# 9. 添加性能监控器
git add era5_performance_monitor.py
git commit -m "feat(monitor): 添加性能监控器GUI应用

核心功能：
- 实时监控下载速度和累计下载量
- SQLite数据库持久化存储
- 三种图表：速度曲线、累计下载量、系统资源
- 双Y轴显示（GB/MB）
- 智能单位格式化
- 网络接口I/O监控
"

# 10. 添加性能监控器启动脚本
git add "启动性能监控.bat"
git commit -m "feat: 添加性能监控器启动脚本"

# 11. 添加v3.0
git add ERA5download_GUI_v3.py
git commit -m "feat(v3.0): 添加高级特性集成版下载器"

# 12. 添加v3.0启动脚本
git add "启动程序_v3.bat"
git commit -m "feat: 添加v3.0启动脚本"

# 13. 添加v3.0文档
git add \
  "v3.0新功能说明.md" \
  "v3.0性能优化说明.md"

git commit -m "docs(v3.0): 添加v3.0完整功能文档"

# 14. 添加v3.1
git add ERA5_AutoRecovery.py
git commit -m "feat(v3.1): 添加独立智能自动恢复系统"

# 15. 添加v3.1相关文件
git add "启动智能恢复版.bat"
git add "智能自动恢复功能说明.md"
git commit -m "feat: 添加v3.1独立监控版启动脚本和文档"

# 16. 添加高级特性代码
git add era5_advanced_features.py
git commit -m "feat: 添加高级特性模块"

# 17. 添加相关文档
git add \
  "网络接口监控说明.md" \
  "智能恢复功能Bug修复说明.md" \
  "版本对比指南_v2.1_vs_v3.1.md"

git commit -m "docs: 添加监控和版本对比文档"

# 18. 推送分支
git push -u origin feature/advanced-features-v2
```

---

#### 第四阶段：主分支文档和工具

```bash
# 19. 切回主分支
git checkout master

# 20. 添加通用文档
git add \
  "README_总览.md" \
  "版本总览README.md" \
  "UV包管理器使用指南.md"

git commit -m "docs: 添加项目总览文档"

# 21. 添加工具脚本
git add \
  setup_env_uv.bat \
  setup_env.bat \
  create_release.py \
  create_release_clean.py \
  check_releases.py \
  check_process.py \
  diagnostic_tool.py

git commit -m "tools: 添加项目工具脚本"

# 22. 添加其他启动脚本
git add \
  "启动v2自动重启版.bat" \
  "启动性能监控.bat" \
  "启动程序_v3.bat" \
  "启动智能恢复版.bat"

git commit -m "feat: 添加所有版本的启动脚本"

# 23. 添加其他文档
git add \
  "监控器修复说明.md" \
  "断点续传监控修复说明.md"

git commit -m "docs: 添加监控和修复说明文档"

# 24. 更新.gitignore
git add .gitignore
git commit -m "chore: 更新.gitignore文件"

# 25. 推送主分支
git push origin master
```

---

## 📊 分支总览

### 分支结构

```
master (主分支)
├── feature/auto-restart-v2
│   ├── v2.1 智能自动重启版
│   ├── 启动脚本
│   └── v2.1 相关文档
│
└── feature/advanced-features-v2
    ├── 性能监控器
    ├── v3.0 高级特性版
    ├── v3.1 独立监控版
    ├── 高级特性模块
    └── 相关文档
```

---

## ⚠️ 注意事项

### 1. 文件编码

某些文件名包含中文，git add 时需要用引号：
```bash
git add "启动v2自动重启版.bat"
```

### 2. .gitignore

确保以下文件不被提交：
- `.era5_auto_recovery_config.json` (配置)
- `era5_performance.db` (数据库)
- `auto_restart_log.json` (日志)
- `download_errors.log` (错误日志)
- `.python-version` (本地环境)

### 3. 大文件

`download_errors.log` 有1MB+，建议：
```bash
# 不提交
echo "download_errors.log" >> .gitignore

# 或者先清空再提交
echo "" > download_errors.log
git add download_errors.log
```

### 4. 分支合并

如果需要合并到主分支：
```bash
# 合并 v2.1 自动重启功能
git checkout master
git merge feature/auto-restart-v2 --no-ff

# 合并高级特性
git merge feature/advanced-features-v2 --no-ff
```

---

## 🎯 推荐的工作流

### 方案1：保持分支独立（推荐）

```
feature/auto-restart-v2 (独立分支)
feature/advanced-features-v2 (独立分支)
```

**优点**：
- ✅ 功能隔离
- ✅ 便于review
- ✅ 用户可以选择使用哪个版本

### 方案2：逐步合并到主分支

```
1. 先合并 feature/auto-restart-v2
2. 再合并 feature/advanced-features-v2
3. 解决冲突
```

**优点**：
- ✅ 主分支包含所有功能
- ✅ 统一的代码库

---

## 🚀 立即执行

如果你想立即开始，我可以帮你：

### 选项A：自动化提交（推荐）

```bash
# 我会生成一个完整的提交脚本
# 你只需要运行它即可
```

### 选项B：手动执行

按照上面的详细步骤，一个个手动提交。

### 选项C：调整计划

如果你有其他想法，我可以调整提交方案。

---

**你想选择哪种方式？**
